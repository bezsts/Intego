In tasks 1 and 2 both FL and QC will need to save LOG files using PROC PRINTTO. QC will need to check FL LOG has correct notes. No datasets are supposed to be compared, just LOG review.
1) Use raw dataset VITALS. Find the highest temperature value within all patients. Print the result to the LOG as a NOTE. Template could be: "NOTE: Highest Temperature is <temperature>, Subject=<subjid>";
2) Use raw dataset AE. Put a WARNING to the LOG in case an AE stop date is empty. Include information about a patient, AE name and AE start date. Template could be: "WARNING: Subject=<subjid>, AE Name=<aename>, AE Start Date=<start_date>";

3) Use raw dataset FOLLOWUP. Count how many days each patient took cocaine, based on COCAIN_D variable. Create a new variable COCDAYS (num, no label). Keep only 2 variables and single record per patient, create USUBJID as in HW1 task 1;
4) Use raw datasets RDMCODE and AE. DO NOT use First and Last statements in this task. Separate patients into 2 datasets by treatment groups. Count how many AEs are in each treatment group: create new variable COUNT (num, no label). Set 2 datasets into 1 to submit this task, keep only XTRTMT and COUNT;
5) Use raw dataset ECG1. Consider Heart Rate characteristics only, others can be dropped. For each patient add 2 new visits with Min and Max Heart Rate, create 2 new variables VISIT and VISITNUM (as per lecture 3 code). Assign VISIT and VISITNUM so that Min Heart Rate would be the 1st record per subject, and Max Heart Rate would be the last record per subject. Suggested values are: VISIT = "Min Heart Rate" and "Max Heart Rate"; VISITNUM = -1 and 999. For other records assign VISIT to VISID and VISITNUM to the day number in VISID. Derive USUBJID, also keep VISITDT and ECG12TM;